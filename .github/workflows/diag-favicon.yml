name: Diagnostic favicon (temporaire)

on:
  workflow_dispatch:

jobs:
  diag:
    runs-on: ubuntu-latest
    steps:
      - name: En-têtes favicon.ico (focalclub.fr)
        run: |
          echo "=== focalclub.fr/favicon.ico ==="
          curl -sSI "https://focalclub.fr/favicon.ico?diag=$(date +%s)" || true
          echo
          echo "=== myfocal.online/favicon.ico ==="
          curl -sSI "https://myfocal.online/favicon.ico?diag=$(date +%s)" || true
          echo
          echo "=== focalclub.fr/index.html (extrait <head>) ==="
          curl -sS "https://focalclub.fr/index.html?diag=$(date +%s)" | grep -i -A2 -B2 "icon\|style.css" || true
