name: Diagnostic temporaire - album Fête de la mer Mesquer vide

on:
  workflow_dispatch:

permissions:
  checks: write

jobs:
  diag:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Vider le cache puis interroger infos-albums.php (liste puis contenu)
        run: |
          {
            echo "=== 0. Vidage du cache disque des albums ==="
            curl -sS --max-time 20 "https://focalclub.fr/espace/diag-vider-cache-albums.php?secret=ba0551506f815f0c8a71ffa5b2ace98b6f4a4b009b3516a4"
            echo ""
            echo ""
            echo "=== 1. Liste des albums (infos-albums.php sans paramètre, juste après le vidage) ==="
            time curl -sS --max-time 30 "https://focalclub.fr/infos-albums.php?bust=$(date +%s)"
            echo ""
            echo ""
            echo "=== 2. Contenu de l'album 2 (Fête de la mer Mesquer 2026), juste après le vidage ==="
            time curl -sS --max-time 30 "https://focalclub.fr/infos-albums.php?album=2&bust=$(date +%s)"
            echo ""
            echo ""
          } > /tmp/resultat.txt 2>&1
          cat /tmp/resultat.txt

      - name: Publier le résultat comme check run
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          CONTENU=$(cat /tmp/resultat.txt | head -c 60000)
          curl -sS -X POST \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/${{ github.repository }}/check-runs \
            -d "$(jq -n --arg sha "${{ github.sha }}" --arg text "$CONTENU" \
              '{name: "resultat-album-mesquer", head_sha: $sha, status: "completed", conclusion: "neutral", output: {title: "Résultat diagnostic album", summary: "voir texte", text: $text}}')" \
            -w "\nHTTP %{http_code}\n"
